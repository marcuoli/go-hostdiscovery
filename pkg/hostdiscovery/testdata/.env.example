# Test Configuration for go-hostdiscovery
# Copy this file to .env and update with your network values
#
# The .env file is ignored by git - your network-specific values stay local

# Windows host with NetBIOS enabled (required for most tests)
TEST_WINDOWS_IP=192.168.1.100

# Expected hostname from NetBIOS (optional - skips validation if empty)
TEST_WINDOWS_HOSTNAME=MYPC

# Expected MAC address in format XX-XX-XX-XX-XX-XX (optional - skips validation if empty)
TEST_WINDOWS_MAC=AA-BB-CC-DD-EE-FF

# Linux host without NetBIOS (optional - some tests will be skipped if empty)
TEST_LINUX_IP=192.168.1.101

# DHCP test configuration (optional - DHCP tests will be skipped if empty)
# Local interface IP - your machine's IP on the network
TEST_DHCP_LOCAL_IP=192.168.1.50
# DHCP server IP - usually your router/gateway
TEST_DHCP_SERVER_IP=192.168.1.1
